# CIS25
week5:
  Class assignments
  Strings and classes
  This program creates a class with a string name and quantity. Two functions that inputs a name and quantity into a file from the main,    and reads and prints out the items from the file.

  Constructor and Destructor
  This programs initializes private attributes through a constructor, then displays them. After that a destructor runs and displays a     message.
week7:
  ifStatements.cpp is a menu using if statements.

  nestedIfStatements was a code I had to debug, which had some errors but fixed them with putting syntax corrections and logic errors      with the if statements and just nested the if statements. It displays the highest number correctly.

Week8:
  A program with a class bankAccount which has functions that are able to add a deposit that adds to the balance and then saves it in a file called transaction. Makes purchase and takes away from balance and saves it, if the purchase is higher then the balance then transaction is denied. Prints the balance and nothing else. Also a function that gets everything from the file and prints it out, so it looks like a bank statement. In int main it calls all the functions with arguments in the parameter.
